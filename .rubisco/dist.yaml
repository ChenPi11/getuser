name: 🛠️ Build distribution package

steps:
  - name: 🛠️ Build wheel distribution
    run: rubisco build
  - name: 📦 Build source distribution
    run: rubisco srcdist --archive-type=zip+tar.gz

